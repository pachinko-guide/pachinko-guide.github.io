# 公開・更新について（GitHub × Netlify 自動デプロイ）

このサイトは **GitHub × Netlify で完全自動デプロイ**になっています。

- 公開URL: **https://rococo-blini-266962.netlify.app**
- GitHubリポジトリ（非公開）: https://github.com/msrk7948-droid/ad-guideline-search
- Netlifyプロジェクト: `rococo-blini-266962`（main ブランチを自動公開）
- 検索よけ（noindex）維持 ＝ URLを知っている人だけ閲覧可

---

## 更新のしかた（あなたの作業はゼロ）

```
あなた「PDF追加して」→ 私が編集＆ git push → Netlifyが自動ビルド＆公開（数十秒）
```

ドラッグ&ドロップはもう不要です。私に頼むだけで、同じURLに自動反映されます。

### 仕組み
1. 私がローカルの `data/` などを編集
2. 私が `git push`（GitHubのmainへ）
3. Netlifyがpushを検知して自動デプロイ → ライブURLに反映

---

## もし自分でも更新したくなったら（任意）

GitHub上でファイルを直接編集してコミットしてもOK（mainブランチ）。
コミットすれば自動でデプロイされます。

---

## 認証・接続のメモ（再設定が必要になった時用）

- GitHubアカウント: `msrk7948-droid`
- ローカルのgit認証: `gh` CLI（`~/.local/bin/gh`）でログイン済み
- Netlify ↔ GitHub: Netlifyの「Build & deploy → Continuous deployment」で
  `ad-guideline-search` リポジトリに接続済み（Auto publishing on）
- 公開ディレクトリ: リポジトリ直下（`netlify.toml` の `publish = "."`）

---

## 旧方式（参考・もう使いません）

以前のNetlify手動ドロップ用zip（`広告宣伝ガイドライン検索_netlify公開用.zip`）は
Git自動デプロイに移行したため不要です。残してありますが使わなくてOKです。
